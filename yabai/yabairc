# ── Scripting Addition ──────────────────────────────────────────────
# https://github.com/asmvik/yabai/wiki/Installing-yabai-(latest-release)#configure-scripting-addition
yabai -m signal --add event=dock_did_restart action="sudo yabai --load-sa"
sudo yabai --load-sa

# ── Layout ──────────────────────────────────────────────────────────
# default layout (can be bsp, stack or float)
yabai -m config layout bsp

# new window spawns to the right if vertical split, or bottom if horizontal split
yabai -m config window_placement second_child

# ── Padding & Gaps ─────────────────────────────────────────────────
# global padding
yabai -m config \
  bottom_padding 8 \
  left_padding 8 \
  right_padding 8 \
  window_gap 8

# per-space top padding (depends on sketchybar; spaces 1-4 = MacBook, 5-7 = 1440p monitor)
if command -v sketchybar &>/dev/null; then
  yabai -m config --space 1 top_padding 8
  yabai -m config --space 2 top_padding 8
  yabai -m config --space 3 top_padding 8
  yabai -m config --space 4 top_padding 38
  yabai -m config --space 5 top_padding 38
  yabai -m config --space 6 top_padding 38
fi

# ── Space Labels ───────────────────────────────────────────────────
yabai -m space 4 --label "[ 1 ]"
yabai -m space 5 --label "[ 2 ]"
yabai -m space 6 --label "[ 3 ]"
yabai -m space 1 --label "[ q ]"
yabai -m space 2 --label "[ w ]"
yabai -m space 3 --label "[ e ]"

# ── Window Appearance ─────────────────────────────────────────────
yabai -m config window_opacity on

# ── Mouse ──────────────────────────────────────────────────────────
# center mouse on window with focus
yabai -m config \
  mouse_follows_focus on \
  focus_follows_mouse autofocus \
  mouse_modifier alt \
  mouse_action1 move \
  mouse_action2 resize \
  mouse_drop_action swap

# ── App Rules (disable tiling) ─────────────────────────────────────
yabai -m rule --add app="^System Settings$" manage=off
yabai -m rule --add app="^Calculator$" manage=off
yabai -m rule --add app="^Karabiner-Elements$" manage=off
yabai -m rule --add app="^Karabiner-EventViewer$" manage=off
yabai -m rule --add app="^QuickTime Player$" manage=off
yabai -m rule --add app="^WireGuard$" manage=off
yabai -m rule --add app="^Bitwarden$" manage=off
yabai -m rule --add app="^Logi Options$" manage=off
yabai -m rule --add app="^SF Symbols$" manage=off
yabai -m rule --add app="^Activity Monitor$" manage=off
yabai -m rule --add app="^WhatsApp$" manage=off
yabai -m rule --add app="^eqMac$" manage=off
yabai -m rule --add app="^Tailscale$" manage=off
yabai -m rule --apply

yabai -m signal --add event=space_changed \
  action="sh ~/.config/sketchybar/plugins/yabai.sh"

echo "Yabai configuration applied."
